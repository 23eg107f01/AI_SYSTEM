{
  "name": "ai-customer-support-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for AI Customer Support",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  }
}
